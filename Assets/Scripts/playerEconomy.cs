// Jakub Fussek, 3.C PVA, Sticker Mania

using UnityEngine;

public class playerEconomy : MonoBehaviour
{
    public static float playerMoney = 400f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
